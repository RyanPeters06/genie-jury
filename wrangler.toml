name = "genie-jury"
main = "worker/index.ts"
compatibility_date = "2026-09-19"

[vars]
OPENAI_MODEL = "gpt-5.6"

[[durable_objects.bindings]]
name = "JURY_SESSIONS"
class_name = "JurySession"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["JurySession"]

# Create the D1 database before deployment, then uncomment and supply its id.
# [[d1_databases]]
# binding = "DB"
# database_name = "genie-jury"
# database_id = "replace-with-wrangler-d1-create-output"

# Create the R2 bucket before deployment, then uncomment this binding.
# [[r2_buckets]]
# binding = "EVIDENCE"
# bucket_name = "genie-jury-evidence"
